// Legacy shim — kept for backward compatibility with .jsx imports.
// All new code should import from ThemeContext (resolves to ThemeContext.tsx).
export { ThemeProvider, useTheme } from './ThemeContext.tsx';
