# 🌍 A KI PRI SA YÉ – API & Frontend

API + Application React (Vite + Tailwind) pour comparer les prix DOM-TOM et vérifier l’état de l’API.

---

## 🚀 Installation

```bash
# Cloner le dépôt
git clone <repo-url>
cd <repo>

# Installer les dépendances
npm install

# Lancer le serveur local
npm run dev

