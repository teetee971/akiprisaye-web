{
  "name": "functions",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "build": "echo '📦 Build en cours...' && rm -rf dist && mkdir dist && cp -r public/* dist/",
    "lint": "eslint .",
    "predeploy": "npm --prefix \"$RESOURCE_DIR\" run lint || true"
  },
  "dependencies": {
    "axios": "1.7.7",
    "firebase-admin": "12.6.0",
    "firebase-functions": "6.0.0"
  },
  "devDependencies": {
    "eslint": "9.34.0"
  }
}
