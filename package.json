{
  "name": "akiprisaye-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A KI PRI SA YÉ – Comparateur de prix DOM-TOM (Cloudflare Pages + Functions).",
  "scripts": {
    "dev": "vite",
    "build": "echo \"📦 Build…\" && rm -rf dist && mkdir -p dist && ( [ -d public ] && cp -r public/* dist/ || true )",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@zxing/browser": "^0.1.5",
    "vite": "^4.5.14"
  }
}
