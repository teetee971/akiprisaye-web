{
  "name": "akiprisaye-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --host",
    "build": "vite build --outDir dist/client",
    "build:client": "vite build --outDir dist/client",
    "preview": "vite preview --host --outDir dist/client --port 4173",
    "check": "tsc --noEmit",
    "start": "npm run preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "typescript": "^5.6.3"
  }
}
